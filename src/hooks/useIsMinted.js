const useIsMinted = (ids, tokensData) => {
    // eslint-disable-next-line no-unused-vars
    const mint = tokensData.map((id, index) => {
        ids.map((a) => {
            id[index] === a
        })
    })


}

export default useIsMinted